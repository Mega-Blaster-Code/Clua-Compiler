#require <test/stdio.hlua>

int function main(int argc, char ** argv)
	for(int i = 0; i < argc; i = i + 1) do
		printInt(i);
		printChar(':');
		printChar(' ');
		printString(argv[i]);
		printChar('\n');
	end

	return 0;
end